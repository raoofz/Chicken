🚀 **MASTER DIRECTIVE: Full Autonomous Deployment to Railway (Senior-Level Execution, Zero-Failure Requirement)**

This is a **critical production system**.
You must operate as a **senior DevOps + backend engineer**, not as an assistant.

Your mission is to **take full control of the project and deploy it to Railway successfully**, fixing ANY issue encountered.

---

# 🎯 CORE OBJECTIVE

You must:

* Analyze the entire project deeply
* Fix all structural, dependency, and runtime issues
* Prepare it for production
* Deploy it fully to Railway
* Ensure it runs online with **zero errors**

---

# 🧠 THINKING MODE (MANDATORY)

You MUST:

* Think step-by-step like a senior engineer
* Never assume anything works — verify everything
* Detect hidden issues before they break deployment
* Fix root causes, not symptoms
* Ensure long-term stability, not temporary fixes

---

# 📦 PROJECT TYPE

* pnpm monorepo
* Multiple apps:

  * apps/api-server
  * apps/poultry-manager
  * apps/mockup-sandbox

❗ Treat each app as an independent deployable service

---

# 🚀 EXECUTION PLAN

## 1. Deep Project Audit

* Validate:

  * pnpm-workspace.yaml
  * All package.json files
  * Dependencies consistency
  * Build scripts
  * Start scripts

* Fix:

  * Broken imports
  * Missing dependencies
  * Version conflicts
  * Invalid scripts

---

## 2. Ensure Build Integrity

You must:

* Run build for each app
* Fix ALL build errors
* Ensure:

  * TypeScript compiles
  * No missing files
  * No runtime crashes

---

## 3. GitHub Integration

* Initialize git (if missing)
* Clean commit history
* Push full project to GitHub

---

## 4. Railway Deployment (STRICT)

Create **3 services**:

---

### 🔹 api-server

* Root Directory: apps/api-server
* Build:
  pnpm install --no-frozen-lockfile && pnpm build
* Start:
  pnpm start

---

### 🔹 poultry-manager

* Root Directory: apps/poultry-manager
* Build:
  pnpm install --no-frozen-lockfile && pnpm build
* Start:
  pnpm start

---

### 🔹 mockup-sandbox

* Root Directory: apps/mockup-sandbox
* Build:
  pnpm install --no-frozen-lockfile && pnpm build
* Start:
  pnpm start

---

## 5. Database (Critical)

* Create PostgreSQL in Railway
* Inject DATABASE_URL
* Ensure:

  * Connection works
  * No crashes
  * Queries succeed

---

## 6. Environment System

* Detect ALL required env variables
* Provide defaults if missing
* Ensure app does not crash due to missing config

---

## 7. Port Handling

Backend MUST use:

process.env.PORT

No hardcoded ports allowed

---

## 8. Runtime Validation

You MUST verify:

* Services boot correctly
* No crashes after start
* Logs are clean
* Frontend connects to backend
* API responses work

---

## 9. Debug Loop (CRITICAL)

If ANY failure occurs:

* Analyze logs
* Identify root cause
* Fix it
* Redeploy

🔁 Repeat until 100% success

---

# 🌐 FINAL DELIVERY

You MUST provide:

* Working Railway URL
* Confirmation all services are running
* Confirmation database is connected
* Confirmation no runtime errors exist

---

# ⚠️ STRICT RULES

* No partial work
* No assumptions
* No skipping steps
* No “it should work”

Only:

> Verified, tested, working system

---

# 🧠 FINAL MINDSET

Act as:

> A senior engineer responsible for production deployment under zero-failure tolerance

Failure is NOT acceptable.
