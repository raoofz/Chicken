Farm Intelligence Platform – Full Engineering Request (Production Level)

I need you to transform my existing poultry farm management system into a full Farm Intelligence Platform (10/10 level).

This is NOT a simple CRUD system.
This must become a self-learning decision-making system.

⸻

1. Architecture (Mandatory)
	•	Use Clean Architecture
	•	Separate:
	•	Controller Layer
	•	Service Layer
	•	Data Layer
	•	Intelligence Layer (AI Engine)
	•	No business logic inside controllers

⸻

2. Database (PostgreSQL – Required)

Implement or refactor database with the following tables:
	•	farms
	•	flocks
	•	production_logs
	•	feed_logs
	•	water_logs
	•	environment_logs
	•	mortality_logs
	•	cost_logs

Requirements:
	•	All tables must include timestamps
	•	Proper foreign key constraints
	•	Time-series optimized structure

⸻

3. API (REST)

Base: /api/v1/

Endpoints:

POST /production
GET /production

POST /feed
GET /feed

POST /environment
GET /environment

GET /analytics/overview

The analytics endpoint must return:
	•	Average production
	•	Feed efficiency
	•	Mortality rate
	•	Alerts

⸻

4. Intelligence Layer (Critical)

A. Rule Engine
	•	Detect inefficiencies automatically

Example:
IF feed increases AND production decreases → Trigger alert

⸻

B. Prediction Engine
	•	Predict:
	•	Egg production (next days)
	•	Feed consumption

Use:
	•	Moving average (initial)
	•	Then upgrade to ML model

⸻

C. Anomaly Detection
	•	Detect abnormal drops or spikes

⸻

D. Recommendation Engine

System must suggest actions:

Examples:
	•	“Reduce feed waste”
	•	“Check temperature impact”
	•	“Production below expected”

⸻

E. Self-Learning
	•	System must improve predictions based on historical data

⸻

5. Dashboard
	•	No basic tables only
	•	Must include:
	•	Charts (production trends)
	•	KPIs
	•	Alerts panel

⸻

6. Performance
	•	API response < 1 second
	•	Use caching (Redis)
	•	Background jobs for AI (queue system)

⸻

7. Tech Stack (Recommended)

Backend:
	•	Node.js (NestJS) OR Python (FastAPI)

Database:
	•	PostgreSQL

AI:
	•	Python (pandas, sklearn)

Queue:
	•	BullMQ or Celery

⸻

8. Final Requirement

System must:
	•	Analyze data automatically
	•	Generate alerts without user asking
	•	Provide actionable insights

This is NOT optional.

⸻

Goal:

Build a system that thinks like a farm expert.

⸻

Deliverables:
	•	Working backend
	•	Connected database
	•	AI logic implemented
	•	Functional analytics endpoint
	•	Clean, scalable architecture
